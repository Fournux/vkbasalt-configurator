using Gtk 4.0;

Popover openConfigPopover {
    halign: start;
    valign: end;
    width-request: 400;
    Box content {
        orientation: vertical;
        Box {
            halign: center;
            Button {
                label: "Existing config file";
            }
        }
    }
}