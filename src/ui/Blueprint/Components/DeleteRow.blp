using Gtk 4.0;
using Adw 1;

Adw.ActionRow deleteRow {
    [suffix]
    Button deleteButton {
        styles ["circular", "flat"]
        valign: center;
        Adw.ButtonContent {
            icon-name:"close-symbolic";
        }
    }
}